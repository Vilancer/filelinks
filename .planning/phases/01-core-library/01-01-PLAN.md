---
phase: 01-core-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/lib/schema.ts
  - packages/core/src/lib/promptResolver.ts
  - packages/core/src/lib/schema.spec.ts
  - packages/core/src/lib/promptResolver.spec.ts
  - packages/core/src/index.ts
  - packages/core/src/lib/core.ts
  - packages/core/src/lib/core.spec.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-05
user_setup: []
must_haves:
  truths:
    - defineLinks returns { links, config: {} } when config omitted
    - resolvePrompt merges global.prompt and link.prompt with link winning per key
  artifacts:
    - packages/core/src/lib/schema.ts exists and exports all interfaces from CONTEXT
    - packages/core/src/lib/promptResolver.ts exists
  key_links:
    - packages/core/src/index.ts re-exports schema + promptResolver public symbols
---

<objective>
Implement **Step 2** of Phase 1: canonical **schema** (`PromptConfig`, `FileLinkConfig`, `FileLinkEntry`, `AffectedFile`, `defineLinks`) and **`resolvePrompt`** merge helper per `.planning/phases/01-core-library/01-CONTEXT.md`.

**Output:** Types + pure functions + Vitest coverage; remove placeholder `core()` stub.

**Replan (2026-04-02):** Updated Task 2 per `.planning/phases/01-core-library/01-REVIEWS.md` — defensive `?? {}` before spread so optional `prompt` objects never spread `undefined` (TypeScript-safe).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-library/01-CONTEXT.md
@.planning/phases/01-core-library/01-REVIEWS.md
@packages/core/tsconfig.lib.json
@packages/core/src/index.ts
@packages/core/src/lib/core.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema.ts with locked public types</name>
  <files>packages/core/src/lib/schema.ts</files>
  <read_first>.planning/phases/01-core-library/01-CONTEXT.md,packages/core/tsconfig.lib.json</read_first>
  <action>Create `packages/core/src/lib/schema.ts`. Export exactly these named exports matching 01-CONTEXT (field names and optionality must match): `PromptConfig` (systemPrompt?, temperature?, maxTokens?), `FileLinkConfig` (prompt?), `FileLinkEntry` (trigger, affects, severity?, prompt?), `AffectedFile` (file, reason). Implement `defineLinks(links, config?)` to return `{ links, config: config ?? {} }`. Do not add runtime validation libraries unless specified later.</action>
  <verify>pnpm exec nx run core:test --skip-nx-cache</verify>
  <acceptance_criteria>
    - `packages/core/src/lib/schema.ts` contains the string `export interface PromptConfig`
    - `packages/core/src/lib/schema.ts` contains `return { links, config: config ?? {} }`
    - `packages/core/src/lib/schema.ts` contains `export function defineLinks`
  </acceptance_criteria>
  <done>Schema file exists and exports match CONTEXT</done>
</task>

<task type="auto">
  <name>Task 2: Add promptResolver.ts</name>
  <files>packages/core/src/lib/promptResolver.ts</files>
  <read_first>.planning/phases/01-core-library/01-CONTEXT.md,.planning/phases/01-core-library/01-REVIEWS.md,packages/core/src/lib/schema.ts</read_first>
  <action>Create `packages/core/src/lib/promptResolver.ts` exporting `resolvePrompt(globalConfig: FileLinkConfig, link: FileLinkEntry): PromptConfig` with body: `return { ...(globalConfig.prompt ?? {}), ...(link.prompt ?? {}) };` (shallow spread after defaulting optional prompts to empty object — same merge order as CONTEXT: global first, link overrides). Import types from `./schema`.</action>
  <verify>pnpm exec nx run core:test --skip-nx-cache</verify>
  <acceptance_criteria>
    - `packages/core/src/lib/promptResolver.ts` contains `globalConfig.prompt ?? {}`
    - `packages/core/src/lib/promptResolver.ts` contains `link.prompt ?? {}`
    - `packages/core/src/lib/promptResolver.ts` exports `resolvePrompt`
  </acceptance_criteria>
  <done>Resolver implements CONTEXT merge semantics</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for defineLinks and resolvePrompt</name>
  <files>packages/core/src/lib/schema.spec.ts,packages/core/src/lib/promptResolver.spec.ts</files>
  <read_first>packages/core/src/lib/schema.ts,packages/core/src/lib/promptResolver.ts,packages/core/src/lib/core.spec.ts</read_first>
  <action>Add `schema.spec.ts`: test that `defineLinks([], undefined)` and `defineLinks([], {})` yield `config` equal to `{}` (use deep equality or `Object.keys` length 0). Add `promptResolver.spec.ts`: case (1) global only — link has no prompt; (2) link overrides temperature; (3) empty globals — link prompt only. Remove obsolete `packages/core/src/lib/core.spec.ts` once nothing imports `core`.</action>
  <verify>pnpm exec nx run core:test --skip-nx-cache</verify>
  <acceptance_criteria>
    - `pnpm exec nx run core:test` exits 0
    - `packages/core/src/lib/promptResolver.spec.ts` exists
    - `packages/core/src/lib/schema.spec.ts` exists
  </acceptance_criteria>
  <done>Tests cover CORE-01 and CORE-05 behaviors</done>
</task>

<task type="auto">
  <name>Task 4: Barrel exports and remove stub core</name>
  <files>packages/core/src/index.ts,packages/core/src/lib/core.ts</files>
  <read_first>packages/core/src/index.ts,packages/core/src/lib/core.ts</read_first>
  <action>Update `packages/core/src/index.ts` to `export * from './lib/schema'` and `export * from './lib/promptResolver'`. Delete `packages/core/src/lib/core.ts` entirely (remove `core()` stub). Ensure no remaining imports reference `./lib/core`.</action>
  <verify>pnpm exec nx run core:build --skip-nx-cache; pnpm exec nx run core:test --skip-nx-cache</verify>
  <acceptance_criteria>
    - `packages/core/src/index.ts` does not contain `./lib/core`
    - `grep -r "from './lib/core'" packages/core/src` returns no matches
    - `pnpm exec nx run core:build` exits 0
  </acceptance_criteria>
  <done>Public API is schema + promptResolver only</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run core:build`
- `pnpm exec nx run core:test`
- `pnpm exec nx run core:lint` if configured
</verification>

<success_criteria>
- CORE-01 and CORE-05 satisfied with tests
- No placeholder `core()` remains
</success_criteria>
