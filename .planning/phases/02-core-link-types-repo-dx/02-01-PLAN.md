---
phase: 02-core-link-types-repo-dx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-core-link-types-repo-dx/02-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CORE-06
  - DOC-02

must_haves:
  truths:
    - '`pnpm exec nx run-many -t test-ci` exits 0 for all projects with Vitest targets.'
    - '`pnpm exec nx run-many -t lint` exits 0 (or no lint targets fail).'
    - '`packages/core/src/index.ts` re-exports `LinkType`, `linkType` module symbols.'
    - 'Root `package.json` contains `lint-staged`, `prepare` script `husky`, and `.husky/pre-commit` runs lint-staged then nx test-ci.'
  artifacts:
    - .planning/phases/02-core-link-types-repo-dx/02-VERIFICATION.md
  key_links:
    - 'CORE-06 ↔ schema.ts + linkType.ts + tests'
    - 'DOC-02 ↔ ARCHITECTURE.md + CONTRIBUTING.md + AGENTS.md + .cursor/rules'
---

<objective>
Close Phase 2 by **verifying** that **CORE-06** and **DOC-02** are satisfied in the working tree: automated tests and lint pass, required files and exports exist, and a short **02-VERIFICATION.md** records evidence. Update **STATE.md** to reflect Phase 2 complete.

Purpose: Roadmap Phase 2 is implementation-heavy and already merged; execution is primarily **regression + traceability**, not new features.

Output: `02-VERIFICATION.md`, updated `STATE.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-link-types-repo-dx/02-CONTEXT.md
@packages/core/src/lib/schema.ts
@packages/core/src/lib/linkType.ts
@packages/core/src/index.ts
@package.json
@.husky/pre-commit
@CONTRIBUTING.md
@AGENTS.md
@.cursor/rules/filelinks-architecture.mdc
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint and test-ci for the workspace</name>
  <files></files>
  <read_first>
    - `package.json` — scripts and lint-staged block
    - `nx.json` — plugin targets
  </read_first>
  <action>
From repo root, run exactly:
`NX_CLOUD=false pnpm exec nx run-many -t lint --parallel=3`
then
`NX_CLOUD=false pnpm exec nx run-many -t test --parallel=3`  
(Use `test` if `test-ci` refuses without Nx Agents / Nx Cloud.)

Minimum: **core** tests must pass: `NX_CLOUD=false pnpm exec nx run core:test` or `core:test-ci` when allowed.
</action>
<acceptance_criteria> - Last command in the chain for tests ends with **exit code 0**. - Capture command names and exit codes in `02-VERIFICATION.md` § "Commands run".
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Grep-verify CORE-06 exports and types</name>
  <files>
    - packages/core/src/lib/schema.ts
    - packages/core/src/lib/linkType.ts
    - packages/core/src/index.ts
    - packages/core/src/lib/schema.spec.ts
    - packages/core/src/lib/linkMatcher.spec.ts
    - packages/core/src/lib/linkType.spec.ts
  </files>
  <read_first>
    - `packages/core/src/index.ts` — must contain `export * from './lib/linkType'`
    - `packages/core/src/lib/schema.ts` — must export `LinkType` type and `linkType?` on `FileLinkEntry`
  </read_first>
  <action>
Verify by reading files (not assumptions):
1. `LinkType` is exported from `schema.ts` (or re-exported via barrel only — document which).
2. `FileLinkEntry` includes optional `linkType?: LinkType`.
3. `linkType.ts` exports `LINK_TYPES`, `LINK_TYPE_DESCRIPTIONS`, `isLinkType`.
4. Barrel `index.ts` re-exports `./lib/linkType`.
5. Spec files `schema.spec.ts`, `linkMatcher.spec.ts`, `linkType.spec.ts` exist and reference `linkType` where applicable.

Record file:line notes in `02-VERIFICATION.md` § "CORE-06".
</action>
<acceptance_criteria> - `02-VERIFICATION.md` contains a bullet list naming each check with **pass** or **fail**; all must be **pass** before phase close. - `grep -n "export.*LinkType" packages/core/src/lib/schema.ts` shows `LinkType` type export. - `grep -n "linkType" packages/core/src/lib/schema.ts` shows `linkType?` on `FileLinkEntry`.
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify DOC-02 docs and agent artifacts</name>
  <files>
    - .planning/codebase/ARCHITECTURE.md
    - CONTRIBUTING.md
    - AGENTS.md
    - .cursor/rules/filelinks-architecture.mdc
  </files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 2 success criteria (2)–(4)
  </read_first>
  <action>
Confirm each file exists and contains:
- **ARCHITECTURE.md**: mentions `linkType` and config → matcher flow.
- **CONTRIBUTING.md**: packages list, Nx/pnpm commands table, Husky / lint-staged mention.
- **AGENTS.md**: `linkType` variants and nx commands.
- **filelinks-architecture.mdc**: packages + `linkType` + commands.

Add checklist to `02-VERIFICATION.md` § "DOC-02".
</action>
<acceptance_criteria> - Each of the four paths exists (test with `test -f` or Read tool). - `02-VERIFICATION.md` lists all four with **pass**.
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verify Husky pre-commit script</name>
  <files>
    - .husky/pre-commit
    - package.json
  </files>
  <read_first>
    - `.husky/pre-commit`
    - `package.json` → `lint-staged`, `prepare`, `devDependencies` include `husky` and `lint-staged`
  </read_first>
  <action>
1. Confirm `package.json` has `"prepare": "husky"` and a `lint-staged` object.
2. Confirm `pnpm-lock.yaml` includes package entries for `husky` and `lint-staged` (e.g. `pnpm why husky` or grep lockfile).
3. Confirm `.husky/pre-commit` contains `lint-staged` and `nx run-many -t test-ci` (or equivalent).

Document in `02-VERIFICATION.md` § "Husky".
</action>
<acceptance_criteria> - `.husky/pre-commit` file contains both `lint-staged` and `test-ci` strings. - `package.json` contains `"husky"` in devDependencies (exact key `husky`).
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Write 02-VERIFICATION.md and update STATE.md</name>
  <files>
    - .planning/phases/02-core-link-types-repo-dx/02-VERIFICATION.md
    - .planning/STATE.md
  </files>
  <read_first>
    - `.planning/STATE.md` — current frontmatter schema
  </read_first>
  <action>
Create `02-VERIFICATION.md` with sections: Summary, Commands run, CORE-06, DOC-02, Husky, Sign-off (date).

Update `.planning/STATE.md`:

- `status`: `planning` or `executing` → appropriate value when phase verified (e.g. `planning` with `stopped_at` noting Phase 2 verified, or move to Phase 3 focus).
- `progress.completed_phases`: **2** (if Phase 2 verified complete).
- `progress.percent`: recompute from `completed_phases / total_phases` (3 phases → ~67%).
- `stopped_at`: short string that Phase 2 verification done, next Phase 3 CLI.

Keep `gsd_state_version` unchanged.
</action>
<acceptance_criteria> - `02-VERIFICATION.md` exists and is non-empty with all sections. - `STATE.md` frontmatter `completed_phases` is **2** and `progress.total_phases` is **3**. - `STATE.md` body "Current focus" or equivalent references **Phase 3 — CLI MVP** as next.
</acceptance_criteria>
</task>

</tasks>

<verification>
Phase 2 roadmap success criteria (`.planning/ROADMAP.md`) are satisfied or gaps are explicitly listed in `02-VERIFICATION.md` with owner/next step.
</verification>

<success_criteria>

- `02-VERIFICATION.md` complete with evidence.
- Tests and lint pass (recorded).
- STATE reflects Phase 2 complete and Phase 3 next.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-link-types-repo-dx/02-01-SUMMARY.md` per execute-phase convention.
</output>
