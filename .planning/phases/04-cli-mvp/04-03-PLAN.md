---
phase: 04-cli-mvp
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - packages/cli/package.json
  - packages/cli/project.json
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - CLI-04
  - DOC-01
user_setup: []
must_haves:
  truths:
    - '`packages/cli/package.json` declares a `bin` field pointing at the built CLI entry so `npx filelinks` works after publish (D-17).'
    - 'Root `README.md` includes install, minimal `filelinks.config.ts`, and `check` / `list` / `add` usage (DOC-01).'
  artifacts:
    - packages/cli/package.json
    - README.md
  key_links:
    - 'CLI-04 ↔ package.json bin + metadata'
    - 'DOC-01 ↔ README.md'
---

<objective>
Finish **publishability** and **documentation**: wire **`bin`** and package metadata for the **`filelinks`** npm package (**CLI-04**, **D-17**) and update the **root `README.md`** with **install**, a **minimal `filelinks.config.ts`**, and **`check` / `list` / `add`** examples (**DOC-01**). Optionally add a short pointer in **`CONTRIBUTING.md`** if needed for local **`nx build`** / **`node dist/...`** smoke (do not duplicate the full architecture map).

**Depends on:** wave **02** CLI implementation exists.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cli-mvp/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@packages/cli/project.json
@packages/cli/package.json
@README.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `bin`, `main`, `types`, and publish `files` for `filelinks`</name>
  <files>packages/cli/package.json,packages/cli/project.json</files>
  <read_first>packages/cli/package.json,packages/cli/project.json,.planning/phases/04-cli-mvp/04-CONTEXT.md</read_first>
  <action>
1. Run **`pnpm exec nx run cli:build`** and inspect **`dist/packages/cli/`** (or the configured **`outputPath`**) to find the **actual emitted path** to the CLI entry that executes **`runCli`** (e.g. **`dist/packages/cli/src/index.js`**).
2. Update **`packages/cli/package.json`**:
   - Set **`"main"`**, **`"types"`**, and **`"exports"`** (if used elsewhere) to the **built** outputs under **`dist/...`**, matching Nx **`@nx/js:tsc`** layout (not `src/` paths in the published manifest if the publish root is **`dist/packages/cli`** — align with how **`nx-release-publish`** expects **`packageRoot`** per **`project.json`**).
   - Add **`"bin"`**: map command name **`filelinks`** to the **compiled** JS entry path **relative to the published package root**.
   - Add **`"files"`** (if missing) so the publish tarball includes **`dist`** and **`README.md`**.
3. If **`project.json`** needs an extra **`assets`** or **`package.json`** copy step for release, add the minimal change so **`pnpm exec nx release publish`** (future) sees a valid **`package.json`** in **`dist/packages/cli`**.

**Concrete target state (adjust paths to match disk after build):** `"bin": { "filelinks": "./src/index.js" }` **only if** publish root is `dist/packages/cli` and that file exists there; otherwise set the correct relative path observed on disk.

  </action>
  <verify>pnpm exec nx run cli:build --skip-nx-cache && node "$(node -e "const p=require('./packages/cli/project.json'); console.log(p.targets.build.options.outputPath)")/src/index.js" --version</verify>
  <acceptance_criteria>
    - `packages/cli/package.json` contains `"bin"` with key `filelinks`
    - After `pnpm exec nx run cli:build`, `node dist/packages/cli/src/index.js --version` OR the verified bin path prints a semver string (same as package version)
  </acceptance_criteria>
  <done>CLI-04 bin wiring validated locally</done>
</task>

<task type="auto">
  <name>Task 2: Root README for DOC-01</name>
  <files>README.md</files>
  <read_first>README.md,.planning/REQUIREMENTS.md,.planning/phases/04-cli-mvp/04-CONTEXT.md</read_first>
  <action>
Replace/expand **root `README.md`** so it includes **all** of:

1. **One-line description** + link to repo purpose (may keep existing tagline).
2. **Install** — `pnpm add -D filelinks` **or** `npm install filelinks` (document both; note package name **`filelinks`** on npm).
3. **Minimal `filelinks.config.ts` example** using **`defineLinks`** from **`@filelinks/core`** (or note that published CLI package re-exports if you add a re-export — prefer showing **`import { defineLinks } from '@filelinks/core'`** + `export default defineLinks([...], {})` per **`01-CONTEXT.md`**).
4. **Commands:** `filelinks check`, `filelinks list`, `filelinks add` with **one example each** (mention **`--cwd`**, **`--config`**, **`--json`** on **`check`/`list`** per decisions). Note that **`add`** is an **interactive Ink** terminal UI (**D-19**) with **searchable file/path picks** — not plain stdin line prompts.
5. **Local dev** — `pnpm exec nx run cli:build` then `node dist/packages/cli/...` or `pnpm exec nx run filelinks:build` per actual Nx project name (**verify** with `nx show project filelinks` or **`packages/cli/project.json` name**). Mention **React/Ink** only if relevant to contributors (e.g. where UI code lives).

Keep it concise (DOC-01 is MVP documentation, not a manual).

  </action>
  <verify>grep -E "filelinks check|defineLinks|filelinks.config" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains the substring `defineLinks`
    - `README.md` contains the substring `filelinks check`
    - `README.md` contains the substring `filelinks list`
    - `README.md` contains the substring `filelinks add`
  </acceptance_criteria>
  <done>DOC-01 satisfied</done>
</task>

</tasks>
