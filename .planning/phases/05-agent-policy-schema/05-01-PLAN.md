---
phase: 05-agent-policy-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/lib/schema.ts
  - packages/core/src/lib/schema.spec.ts
  - packages/core/src/lib/agentRunPolicy.ts
  - packages/core/src/lib/agentRunPolicy.spec.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - AGENT-01
user_setup: []
must_haves:
  truths:
    - '`defineLinks` rejects invalid `agent.runPolicy` values at decode time.'
    - 'Global `config.agent.runPolicy` and per-link `entry.agent.runPolicy` override merge via `resolveAgentRunPolicy`.'
    - 'Omitted policy at both levels resolves to `trigger-or-affects`.'
    - '`pnpm exec nx run core:test` and `core:build` exit 0.'
  artifacts:
    - packages/core/src/lib/agentRunPolicy.ts
    - packages/core/src/lib/schema.ts
  key_links:
    - 'AGENT-01 ↔ AgentRunPolicySchema + FileLinkConfig/Entry agent field'
    - 'AGENT-01 ↔ resolveAgentRunPolicy mirrors resolvePrompt'
---

<objective>
Implement **AGENT-01**: Effect Schema for agent run policy (`trigger-or-affects` only), optional **`agent`** settings on global config and per-link entry, and **`resolveAgentRunPolicy`** with global → per-link override and default **`trigger-or-affects`**.

**Out of scope:** `classifyStagedLinks`, `shouldRunAgentForLink`, CLI changes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-policy-schema/05-CONTEXT.md
@.planning/phases/05-agent-policy-schema/05-RESEARCH.md
@packages/core/src/lib/schema.ts
@packages/core/src/lib/promptResolver.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AgentRunPolicy and AgentSettings schemas</name>
  <files>packages/core/src/lib/schema.ts</files>
  <read_first>packages/core/src/lib/schema.ts,.planning/phases/05-agent-policy-schema/05-CONTEXT.md</read_first>
  <action>
In `schema.ts`, add and **export**:

1. `AgentRunPolicySchema = Schema.Literal('trigger-or-affects')` — **only** this literal (D-10).
2. `AgentSettingsSchema = Schema.Struct({ runPolicy: Schema.optional(AgentRunPolicySchema) })`.
3. Extend **`FileLinkConfigSchema`** with `agent: Schema.optional(AgentSettingsSchema)`.
4. Extend **`FileLinkEntrySchema`** with `agent: Schema.optional(AgentSettingsSchema)`.
5. Export type aliases: `AgentRunPolicy`, `AgentSettings` from the schemas (same `Schema.Type` style as existing types in this file).

Do **not** remove or rename existing fields (`prompt`, `trigger`, `affects`, `linkType`, `severity`).
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `packages/core/src/lib/schema.ts` contains `AgentRunPolicySchema` - `packages/core/src/lib/schema.ts` contains literal string `'trigger-or-affects'` - `FileLinkEntrySchema` struct includes `agent` field definition - `FileLinkConfigSchema` struct includes `agent` field definition
</acceptance_criteria>
<done>AGENT-01 schema fields defined</done>
</task>

<task type="auto">
  <name>Task 2: Schema specs for valid and invalid runPolicy</name>
  <files>packages/core/src/lib/schema.spec.ts</files>
  <read_first>packages/core/src/lib/schema.spec.ts,packages/core/src/lib/schema.ts</read_first>
  <action>
Add Vitest cases:

1. **`defineLinks`** accepts entry + config with `agent: { runPolicy: 'trigger-or-affects' }` at global and per-link levels.
2. **`defineLinks`** throws (decode failure) when `runPolicy` is `'trigger-only'` or any string other than `'trigger-or-affects'`.

Use `expect(() => defineLinks(...)).toThrow()` for invalid case.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `schema.spec.ts` contains substring `trigger-or-affects` - `schema.spec.ts` contains a test that expects throw for invalid policy (e.g. `trigger-only`)
</acceptance_criteria>
<done>Invalid policy fails decode in tests</done>
</task>

<task type="auto">
  <name>Task 3: resolveAgentRunPolicy helper</name>
  <files>packages/core/src/lib/agentRunPolicy.ts,packages/core/src/lib/agentRunPolicy.spec.ts,packages/core/src/index.ts</files>
  <read_first>packages/core/src/lib/promptResolver.ts,packages/core/src/lib/schema.ts</read_first>
  <action>
Create `packages/core/src/lib/agentRunPolicy.ts`:

```ts
export function resolveAgentRunPolicy(
  globalConfig: FileLinkConfig,
  link: FileLinkEntry,
): AgentRunPolicy {
  return (
    link.agent?.runPolicy ??
    globalConfig.agent?.runPolicy ??
    'trigger-or-affects'
  );
}
```

Import types from `./schema`.

Add `agentRunPolicy.spec.ts` with cases:

- per-link override wins over global
- global used when per-link omitted
- both omitted → `'trigger-or-affects'`

Export from `packages/core/src/index.ts`: `export * from './lib/agentRunPolicy'` and ensure `AgentRunPolicy` type is reachable via schema barrel (already exported from schema).
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `agentRunPolicy.ts` exports `resolveAgentRunPolicy` - `agentRunPolicy.ts` contains `?? 'trigger-or-affects'` - `packages/core/src/index.ts` contains `agentRunPolicy` - `agentRunPolicy.spec.ts` has at least 3 `it(` blocks
</acceptance_criteria>
<done>AGENT-01 resolver exported and tested</done>
</task>

</tasks>

<verification>
Run from repo root:
- `pnpm exec nx run core:test --skip-nx-cache`
- `pnpm exec nx run core:build --skip-nx-cache`

Manual: `defineLinks` with invalid `runPolicy` in a scratch object throws before return.
</verification>

<success_criteria>

- All tasks acceptance criteria pass
- AGENT-01 traceable to schema + resolver + specs
  </success_criteria>
