---
phase: 05-agent-policy-schema
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - packages/core/src/lib/agentRunPolicy.ts
  - packages/core/src/lib/agentRunPolicy.spec.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - AGENT-03
user_setup: []
must_haves:
  truths:
    - '`shouldRunAgentForLink` returns true for `trigger-or-affects` when either trigger or affect side matched.'
    - 'Returns false when neither side matched.'
    - 'Works with resolved policy from `resolveAgentRunPolicy` in integration-style tests.'
  artifacts:
    - packages/core/src/lib/agentRunPolicy.ts
  key_links:
    - 'AGENT-03 ↔ StagedLinkCoverage from stagedClassifier'
    - 'AGENT-03 ↔ AgentRunPolicy from schema'
---

<objective>
Implement **AGENT-03**: **`shouldRunAgentForLink(coverage, resolvedPolicy)`** implementing **`trigger-or-affects`** gate per D-04–D-06. Wire tests that combine classification + policy resolution.

**Depends on:** Plan 01 (policy types/resolver), Plan 02 (`StagedLinkCoverage`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-policy-schema/05-CONTEXT.md
@.planning/phases/05-agent-policy-schema/05-RESEARCH.md
@packages/core/src/lib/agentRunPolicy.ts
@packages/core/src/lib/stagedClassifier.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shouldRunAgentForLink</name>
  <files>packages/core/src/lib/agentRunPolicy.ts</files>
  <read_first>packages/core/src/lib/agentRunPolicy.ts,packages/core/src/lib/stagedClassifier.ts,.planning/phases/05-agent-policy-schema/05-CONTEXT.md</read_first>
  <action>
In `agentRunPolicy.ts`, add:

```ts
import type { StagedLinkCoverage } from './stagedClassifier';

export function shouldRunAgentForLink(
  coverage: StagedLinkCoverage,
  policy: AgentRunPolicy,
): boolean {
  switch (policy) {
    case 'trigger-or-affects':
      return coverage.triggerMatched || coverage.affectMatched;
    default: {
      const _exhaustive: never = policy;
      return _exhaustive;
    }
  }
}
```

Use `AgentRunPolicy` type from `./schema`. Do **not** re-derive matching from staged paths inside this function (D-04).
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `agentRunPolicy.ts` exports `shouldRunAgentForLink` - `agentRunPolicy.ts` contains `coverage.triggerMatched || coverage.affectMatched` - `agentRunPolicy.ts` imports `StagedLinkCoverage` from `./stagedClassifier`
</acceptance_criteria>
<done>Policy gate function implemented</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for shouldRunAgentForLink</name>
  <files>packages/core/src/lib/agentRunPolicy.spec.ts</files>
  <read_first>packages/core/src/lib/agentRunPolicy.spec.ts,packages/core/src/lib/stagedClassifier.ts</read_first>
  <action>
Extend `agentRunPolicy.spec.ts`:

1. **Neither matched:** minimal `StagedLinkCoverage` with `triggerMatched: false`, `affectMatched: false` → `shouldRunAgentForLink(..., 'trigger-or-affects')` is **false**.
2. **Trigger only:** `triggerMatched: true`, `affectMatched: false` → **true**.
3. **Affect only:** `triggerMatched: false`, `affectMatched: true` → **true** (D-06).
4. **Both:** → **true**.

Build coverage objects inline (no need to call `classifyStagedLinks` for these four).
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `agentRunPolicy.spec.ts` contains `shouldRunAgentForLink` - `agentRunPolicy.spec.ts` contains test with `affectMatched: true` and `triggerMatched: false` expecting true - `agentRunPolicy.spec.ts` contains test expecting false when both matched flags false
</acceptance_criteria>
<done>AGENT-03 unit tests complete</done>
</task>

<task type="auto">
  <name>Task 3: Integration test classify + resolve + gate</name>
  <files>packages/core/src/lib/agentRunPolicy.spec.ts</files>
  <read_first>packages/core/src/lib/stagedClassifier.ts,packages/core/src/lib/linkMatcher.spec.ts</read_first>
  <action>
Add one integration-style test in `agentRunPolicy.spec.ts`:

1. Define a `FileLinkEntry` with `trigger: 'greet.ts'`, `linkType: 'file-dir'`, affect `{ file: 'malek', reason: 'test' }`.
2. `classifyStagedLinks(['malek/nested/foo.ts'], [entry])` → take `[0]`.
3. `resolveAgentRunPolicy({}, entry)` → `'trigger-or-affects'`.
4. Assert `shouldRunAgentForLink(coverage, policy) === true` (affect-only staging).
5. Assert `matchStagedLinks(['malek/nested/foo.ts'], [entry])` has length **0**.

Import `classifyStagedLinks` from `./stagedClassifier`, `matchStagedLinks` from `./linkMatcher`.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache && pnpm exec nx run core:build --skip-nx-cache</verify>
<acceptance_criteria> - Integration test exists with `classifyStagedLinks` and `shouldRunAgentForLink` in same `it(` block - `pnpm exec nx run core:build --skip-nx-cache` exits 0
</acceptance_criteria>
<done>End-to-end policy gate verified for affect-only case</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run core:test --skip-nx-cache`
- `pnpm exec nx run core:build --skip-nx-cache`
</verification>

<success_criteria>

- Phase 5 success criteria 3 satisfied in core tests
- All AGENT-\* requirements covered across plans 01–03
  </success_criteria>
