---
phase: 06-provider-system-cursor-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/lib/schema.ts
  - packages/core/src/lib/schema.spec.ts
  - packages/core/src/lib/agentConfigResolver.ts
  - packages/core/src/lib/agentConfigResolver.spec.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - PROV-02
user_setup: []
must_haves:
  truths:
    - '`defineLinks` decodes `agent.provider`, `agent.runtime`, `agent.model`, `agent.local`, `agent.cloud`.'
    - '`resolveAgentConfig` merges global then per-link `agent` settings.'
    - 'Invalid runtime shape (local without cwd, cloud without repos) fails validation with clear error.'
    - 'Omitted `model` resolves to `composer-2.5` for cursor-ready configs.'
    - '`pnpm exec nx run core:test` and `core:build` exit 0.'
  artifacts:
    - packages/core/src/lib/agentConfigResolver.ts
    - packages/core/src/lib/schema.ts
  key_links:
    - 'PROV-02 ↔ AgentSettingsSchema extended fields'
    - 'PROV-02 ↔ resolveAgentConfig mirrors resolvePrompt merge order'
---

<objective>
Implement **PROV-02**: extend **Effect Schema** for agent execution settings and add **`resolveAgentConfig`** + **`validateResolvedAgentConfig`** with explicit runtime rules (no silent defaults for `runtime`/`provider` on merged config used for runs).

**Out of scope:** Provider registry, `@cursor/sdk`, `listModels`, `run`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-provider-system-cursor-sdk/06-CONTEXT.md
@.planning/phases/06-provider-system-cursor-sdk/06-RESEARCH.md
@packages/core/src/lib/schema.ts
@packages/core/src/lib/promptResolver.ts
@packages/core/src/lib/agentRunPolicy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AgentSettings schema</name>
  <files>packages/core/src/lib/schema.ts</files>
  <read_first>packages/core/src/lib/schema.ts,.planning/phases/06-provider-system-cursor-sdk/06-CONTEXT.md</read_first>
  <action>
In `schema.ts`, add and export:

1. `AgentProviderIdSchema = Schema.Literal('cursor')`
2. `AgentRuntimeSchema = Schema.Literal('local', 'cloud')`
3. `AgentLocalSettingsSchema = Schema.Struct({ cwd: Schema.String })`
4. `AgentCloudSettingsSchema = Schema.Struct({ repos: Schema.Array(Schema.String) })`
5. Extend **`AgentSettingsSchema`** with optional: `provider`, `runtime`, `model`, `local`, `cloud` (keep existing `runPolicy`).

Export types: `AgentProviderId`, `AgentRuntime`, `AgentLocalSettings`, `AgentCloudSettings`.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `schema.ts` contains `AgentProviderIdSchema` - `schema.ts` contains literal `'cursor'` - `schema.ts` contains `AgentRuntimeSchema` with `'local'` and `'cloud'` - `AgentSettingsSchema` struct includes `provider` and `runtime` field definitions
</acceptance_criteria>
<done>PROV-02 schema fields defined</done>
</task>

<task type="auto">
  <name>Task 2: Schema specs for agent execution fields</name>
  <files>packages/core/src/lib/schema.spec.ts</files>
  <read_first>packages/core/src/lib/schema.spec.ts,packages/core/src/lib/schema.ts</read_first>
  <action>
Add Vitest cases:

1. `defineLinks` accepts global + per-link `agent: { provider: 'cursor', runtime: 'local', local: { cwd: '.' } }`.
2. `defineLinks` rejects invalid `provider` (e.g. `'openai'`).
3. `defineLinks` rejects invalid `runtime` (e.g. `'hybrid'`).
   </action>
   <verify>pnpm exec nx run core:test --skip-nx-cache</verify>
   <acceptance_criteria> - `schema.spec.ts` contains `provider: 'cursor'` - `schema.spec.ts` contains test expecting throw for invalid provider or runtime
   </acceptance_criteria>
   <done>Invalid provider/runtime fail decode</done>
   </task>

<task type="auto">
  <name>Task 3: resolveAgentConfig and validateResolvedAgentConfig</name>
  <files>packages/core/src/lib/agentConfigResolver.ts,packages/core/src/lib/agentConfigResolver.spec.ts,packages/core/src/index.ts</files>
  <read_first>packages/core/src/lib/promptResolver.ts,packages/core/src/lib/agentRunPolicy.ts,packages/core/src/lib/schema.ts,.planning/phases/06-provider-system-cursor-sdk/06-RESEARCH.md</read_first>
  <action>
Create `agentConfigResolver.ts`:

1. **`ResolvedAgentConfig`** type: merged settings with required `provider` and `runtime` after validation path; `model` always a string after resolve.

2. **`resolveAgentConfig(globalConfig, link)`** — shallow merge `globalConfig.agent` then `link.agent` (per-link wins).

3. **`validateResolvedAgentConfig(merged)`** — throws **`AgentConfigError`** (add minimal class in `errors.ts` in this task OR throw `ConfigValidationError` with code message until plan 02 adds subclass):
   - `provider` required
   - `runtime` required
   - if `runtime === 'local'`: `local?.cwd` required (non-empty string)
   - if `runtime === 'cloud'`: `cloud?.repos` required (non-empty array)
   - if `provider === 'cursor'` and `model` omitted: set default **`composer-2.5`**

4. **`resolveAgentConfig`** calls merge then **`validateResolvedAgentConfig`** and returns validated result.

Add `agentConfigResolver.spec.ts`:

- per-link override wins for `runtime` / `local.cwd`
- local without cwd throws
- cloud without repos throws
- both omitted model → `composer-2.5`

Export from `packages/core/src/index.ts`: `export * from './lib/agentConfigResolver'`.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache && pnpm exec nx run core:build --skip-nx-cache</verify>
<acceptance_criteria> - `agentConfigResolver.ts` exports `resolveAgentConfig` - `agentConfigResolver.ts` exports `validateResolvedAgentConfig` - `agentConfigResolver.ts` contains `composer-2.5` - `index.ts` contains `agentConfigResolver` - `agentConfigResolver.spec.ts` has at least 4 `it(` blocks
</acceptance_criteria>
<done>PROV-02 resolver exported and tested</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run core:test --skip-nx-cache`
- `pnpm exec nx run core:build --skip-nx-cache`
</verification>

<success_criteria>

- All tasks acceptance criteria pass
- PROV-02 traceable to schema + resolver + specs
  </success_criteria>
