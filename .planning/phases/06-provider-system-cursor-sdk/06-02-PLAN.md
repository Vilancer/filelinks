---
phase: 06-provider-system-cursor-sdk
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/core/src/lib/errors.ts
  - packages/core/src/lib/handleError.spec.ts
  - packages/core/src/lib/providers/types.ts
  - packages/core/src/lib/providers/registry.ts
  - packages/core/src/lib/providers/registry.spec.ts
  - packages/core/src/lib/providers/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - PROV-01
  - PROV-03
user_setup: []
must_haves:
  truths:
    - '`AgentProvider` interface defines `id`, `validateCredentials`, `listModels`, `run`.'
    - 'Registry registers and returns providers; unknown id throws typed error.'
    - 'Agent-related `FilelinksError` subclasses exist with stable codes.'
    - '`pnpm exec nx run core:test` and `core:build` exit 0.'
  artifacts:
    - packages/core/src/lib/providers/types.ts
    - packages/core/src/lib/providers/registry.ts
  key_links:
    - 'PROV-01 ↔ getAgentProvider used by future cursorProvider'
    - 'PROV-03 ↔ AgentMissingApiKeyError before SDK calls'
---

<objective>
Implement **PROV-01** provider **interface + registry** and **PROV-03** **typed agent errors** (missing key, unknown provider, config, startup, run failed). No `@cursor/sdk` yet — Cursor provider registered in plan 03.

**Out of scope:** Cursor SDK calls, `resolveAgentConfig` changes (plan 01).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-provider-system-cursor-sdk/06-CONTEXT.md
@.planning/phases/06-provider-system-cursor-sdk/06-RESEARCH.md
@packages/core/src/lib/errors.ts
@packages/core/src/lib/agentConfigResolver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent FilelinksError subclasses</name>
  <files>packages/core/src/lib/errors.ts,packages/core/src/lib/handleError.spec.ts</files>
  <read_first>packages/core/src/lib/errors.ts,packages/core/src/lib/handleError.ts</read_first>
  <action>
Add subclasses extending `FilelinksError` with stable codes:

- **`AgentMissingApiKeyError`** — `AGENT_MISSING_API_KEY` — message mentions `CURSOR_API_KEY` and Cursor Dashboard → Integrations
- **`AgentProviderUnknownError`** — `AGENT_PROVIDER_UNKNOWN`
- **`AgentConfigError`** — `AGENT_CONFIG_INVALID` (use in resolver if not added in 06-01)
- **`AgentStartupError`** — `AGENT_STARTUP_FAILED` — optional `details?: { isRetryable?: boolean }`
- **`AgentRunFailedError`** — `AGENT_RUN_FAILED` — include `runId` in message when provided

Extend `handleError.spec.ts` with one case asserting `normalizeError` returns correct `code` for `AgentMissingApiKeyError`.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `errors.ts` contains `AGENT_MISSING_API_KEY` - `errors.ts` contains `AGENT_PROVIDER_UNKNOWN` - `errors.ts` contains `AGENT_STARTUP_FAILED` - `errors.ts` contains `AGENT_RUN_FAILED` - `handleError.spec.ts` references `AgentMissingApiKeyError` or `AGENT_MISSING_API_KEY`
</acceptance_criteria>
<done>PROV-03 error types defined</done>
</task>

<task type="auto">
  <name>Task 2: AgentProvider types and interface</name>
  <files>packages/core/src/lib/providers/types.ts</files>
  <read_first>packages/core/src/lib/agentConfigResolver.ts,packages/core/src/lib/schema.ts,.planning/phases/06-provider-system-cursor-sdk/06-CONTEXT.md</read_first>
  <action>
Create `providers/types.ts` with:

- **`AgentModelOption`**: `{ id: string; label?: string }`
- **`AgentProviderContext`**: `{ apiKey?: string }`
- **`AgentRunInput`**: `{ prompt: string; config: ResolvedAgentConfig; ctx?: AgentProviderContext }`
- **`AgentRunResult`**: `{ status: 'finished'; runId: string; resultText?: string }` (document error path throws `AgentRunFailedError` instead of returning error status)
- **`AgentProvider`** interface: `id`, `validateCredentials(ctx)`, `listModels(ctx): Promise<AgentModelOption[]>`, `run(input): Promise<AgentRunResult>`

Import `ResolvedAgentConfig` from `../agentConfigResolver` and `AgentProviderId` from `../schema`.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache</verify>
<acceptance_criteria> - `providers/types.ts` exports `AgentProvider` - `providers/types.ts` exports `AgentModelOption` - `providers/types.ts` exports `AgentRunInput` - `AgentProvider` includes `listModels`
</acceptance_criteria>
<done>PROV-01 interface defined</done>
</task>

<task type="auto">
  <name>Task 3: Provider registry</name>
  <files>packages/core/src/lib/providers/registry.ts,packages/core/src/lib/providers/registry.spec.ts,packages/core/src/lib/providers/index.ts,packages/core/src/index.ts</files>
  <read_first>packages/core/src/lib/providers/types.ts,packages/core/src/lib/errors.ts</read_first>
  <action>
Create `providers/registry.ts`:

- `registerAgentProvider(provider: AgentProvider): void` — store in `Map` by `provider.id`
- `getAgentProvider(id: AgentProviderId): AgentProvider` — throw **`AgentProviderUnknownError`** if missing
- `listAgentProviderIds(): AgentProviderId[]`

Create `providers/index.ts` exporting registry + types; export **`registerBuiltInProviders()`** as no-op stub (or empty) until plan 03 registers Cursor.

Add `registry.spec.ts`: register mock provider, `get` returns it, unknown id throws.

Export from `packages/core/src/index.ts`:

- `export * from './lib/providers'`
- helper **`listAgentModels(providerId, ctx?)`** → `getAgentProvider(id).listModels(ctx ?? {})`

Do **not** add `@cursor/sdk` dependency in this plan.
</action>
<verify>pnpm exec nx run core:test --skip-nx-cache && pnpm exec nx run core:build --skip-nx-cache</verify>
<acceptance_criteria> - `registry.ts` exports `getAgentProvider` - `registry.ts` exports `registerAgentProvider` - `registry.spec.ts` contains `AgentProviderUnknownError` or `AGENT_PROVIDER_UNKNOWN` - `index.ts` contains `listAgentModels` or `providers`
</acceptance_criteria>
<done>PROV-01 registry exported and tested</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run core:test --skip-nx-cache`
- `pnpm exec nx run core:build --skip-nx-cache`
</verification>

<success_criteria>

- PROV-01 and agent error codes ready for Cursor provider (plan 03)
  </success_criteria>
