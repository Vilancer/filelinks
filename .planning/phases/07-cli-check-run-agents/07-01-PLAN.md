---
phase: 07-cli-check-run-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/lib/gitReader.ts
  - packages/core/src/lib/gitReader.spec.ts
  - packages/core/src/lib/agentPrompt.ts
  - packages/core/src/lib/agentPrompt.spec.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - CLI-07
must_haves:
  truths:
    - 'Staged trigger paths produce a unified diff string via git.'
    - 'All declared affect paths are read from disk (or placeholder when missing).'
    - 'buildAgentPrompt returns one string with system prompt, trigger diff, and affect file sections.'
  artifacts:
    - path: packages/core/src/lib/gitReader.ts
      provides: getStagedDiffForPaths
      contains: getStagedDiffForPaths
    - path: packages/core/src/lib/agentPrompt.ts
      provides: readAffectedContents and buildAgentPrompt
      contains: buildAgentPrompt
  key_links:
    - from: packages/core/src/lib/agentPrompt.ts
      to: packages/core/src/lib/promptResolver.ts
      via: PromptConfig sections in prompt string
      pattern: systemPrompt|Staged changes|Affected files
    - from: packages/core/src/index.ts
      to: packages/core/src/lib/agentPrompt.ts
      via: barrel export
      pattern: agentPrompt|buildAgentPrompt
---

<objective>
Add **core** helpers for CLI-07: staged trigger diff, affected file reads, and **`buildAgentPrompt`** composition (testable, no CLI/SDK imports).

**Out of scope:** `runCheck` orchestration, Commander flag, docs (plans 07-02–07-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-check-run-agents/07-RESEARCH.md
@.planning/STATE.md
@packages/core/src/lib/gitReader.ts
@packages/core/src/lib/promptResolver.ts
@packages/core/src/lib/stagedClassifier.ts
@packages/core/src/index.ts
</context>

<interfaces>
From `packages/core/src/lib/stagedClassifier.ts`:
```typescript
export interface StagedLinkCoverage {
  entry: FileLinkEntry;
  triggerMatched: boolean;
  affectMatched: boolean;
  triggerPaths: string[];
  affectPaths: string[];
  missingAffected: AffectedFile[];
}
```

From `packages/core/src/lib/promptResolver.ts`:

```typescript
export function resolvePrompt(
  globalConfig: FileLinkConfig,
  link: FileLinkEntry,
): PromptConfig;
```

From `packages/core/src/lib/providers/types.ts`:

```typescript
export type AgentRunInput = {
  prompt: string;
  config: ResolvedAgentConfig;
  ctx?: AgentProviderContext;
};
```

Phase 7 consumes **`buildAgentPrompt` → `prompt` string** only.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getStagedDiffForPaths in gitReader</name>
  <files>packages/core/src/lib/gitReader.ts,packages/core/src/lib/gitReader.spec.ts</files>
  <read_first>packages/core/src/lib/gitReader.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <behavior>
    - Empty `paths` → returns `''` without calling git.
    - Non-empty paths → `execFileSync('git', ['diff', '--cached', '--', ...paths], { cwd, encoding: 'utf8' })`.
    - Git failure → error wrapped for `normalizeError` (same style as `getStagedFilePaths`).
  </behavior>
  <action>
Add **`getStagedDiffForPaths(cwd: string, repoRelativePaths: string[]): string`** to `gitReader.ts` per research Pattern 2.

In **`gitReader.spec.ts`** (create if missing): mock `node:child_process` `execFileSync` via `vi.spyOn` or `vi.mock`; assert empty paths skips exec; assert git argv includes `diff`, `--cached`, and path args.

Do **not** change `getStagedFilePaths` behavior.
</action>
<verify>
<automated>pnpm exec nx run core:test --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/core/src/lib/gitReader.ts` contains `export function getStagedDiffForPaths` - `packages/core/src/lib/gitReader.ts` contains `'diff', '--cached'` - `packages/core/src/lib/gitReader.spec.ts` exists and references `getStagedDiffForPaths`
</acceptance_criteria>
<done>Staged diff helper exported and unit-tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: readAffectedContents and buildAgentPrompt</name>
  <files>packages/core/src/lib/agentPrompt.ts,packages/core/src/lib/agentPrompt.spec.ts</files>
  <read_first>packages/core/src/lib/schema.ts,packages/core/src/lib/stagedClassifier.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <behavior>
    - `readAffectedContents(cwd, affects[])` reads each `aff.file` under `cwd` (repo-relative); missing file → placeholder line `(file not found at …)`; directory paths → skip with note (no tree walk in v1.1).
    - Optional: truncate single file content &gt; 256 KiB with `…(truncated)` suffix.
    - `buildAgentPrompt({ prompt, coverage, triggerDiff, affectedFiles })` includes: system/instructions from `PromptConfig.systemPrompt`, metadata lines for `temperature`/`maxTokens` when set, link trigger + affect reasons, `## Staged changes (trigger)` (explicit “No trigger files staged” when diff empty and `triggerPaths` empty), `## Affected files` with fenced blocks per file.
    - Affect-only: still builds prompt when `triggerDiff` is empty.
  </behavior>
  <action>
Create **`packages/core/src/lib/agentPrompt.ts`**:

1. **`export type AffectedFileContent = { path: string; reason: string; content: string }`**

2. **`export function readAffectedContents(cwd: string, affects: AffectedFile[]): AffectedFileContent[]`** — use `node:fs` `readFileSync` for files; `statSync` to detect directories.

3. **`export function buildAgentPrompt(input: { prompt: PromptConfig; coverage: StagedLinkCoverage; triggerDiff: string; affectedFiles: AffectedFileContent[] }): string`** — single markdown-ish string per research Pattern 4; no `@cursor/sdk`.

Add **`agentPrompt.spec.ts`**: missing file placeholder; `buildAgentPrompt` contains `systemPrompt` text and affect path; empty trigger diff section present for affect-only coverage fixture.
</action>
<verify>
<automated>pnpm exec nx run core:test --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/core/src/lib/agentPrompt.ts` contains `export function readAffectedContents` - `packages/core/src/lib/agentPrompt.ts` contains `export function buildAgentPrompt` - `packages/core/src/lib/agentPrompt.spec.ts` contains `file not found` or `not found at` - `packages/core/src/lib/agentPrompt.spec.ts` contains `Staged changes` or `No trigger files staged`
</acceptance_criteria>
<done>CLI-07 prompt assembly lives in core with unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Export new APIs from core barrel</name>
  <files>packages/core/src/index.ts</files>
  <read_first>packages/core/src/index.ts,packages/core/src/lib/agentPrompt.ts,packages/core/src/lib/gitReader.ts</read_first>
  <action>
Export **`getStagedDiffForPaths`** via existing `export * from './lib/gitReader'`.

Add **`export * from './lib/agentPrompt'`** to `packages/core/src/index.ts`.

Run build to ensure public API compiles.
</action>
<verify>
<automated>pnpm exec nx run core:build --skip-nx-cache && pnpm exec nx run core:test --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/core/src/index.ts` contains `agentPrompt` - `pnpm exec nx run core:build --skip-nx-cache` exits 0
</acceptance_criteria>
<done>Core barrel exposes diff + prompt helpers for CLI</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run core:test --skip-nx-cache`
- `pnpm exec nx run core:build --skip-nx-cache`
</verification>

<success_criteria>

- CLI-07 data assembly is complete in `@filelinks/core` with no CLI changes
- Phase 7 plans 07-02+ can import `getStagedDiffForPaths`, `readAffectedContents`, `buildAgentPrompt`
  </success_criteria>

<output>
After completion, create `.planning/phases/07-cli-check-run-agents/07-01-SUMMARY.md`
</output>
