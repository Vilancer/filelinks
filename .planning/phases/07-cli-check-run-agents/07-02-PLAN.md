---
phase: 07-cli-check-run-agents
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/cli/src/lib/runCheck.ts
  - packages/cli/src/lib/runCheck.spec.ts
autonomous: true
requirements:
  - CLI-06
  - CLI-07
must_haves:
  truths:
    - 'Violation reporting unchanged: matchStagedLinks → stdout/JSON → exit on error severity.'
    - 'When runAgents true, agents run for classifyStagedLinks rows passing shouldRunAgentForLink.'
    - 'Trigger staged with missingAffected non-empty still invokes provider.run (CLI-06).'
    - 'Agent eligibility does not use matchStagedLinks rows (D-06).'
    - 'Final exit is max(violationExit, agentExit).'
  artifacts:
    - path: packages/cli/src/lib/runCheck.ts
      provides: async runCheck with agent loop
      contains: classifyStagedLinks
    - path: packages/cli/src/lib/runCheck.spec.ts
      provides: mocked agent orchestration tests
  key_links:
    - from: packages/cli/src/lib/runCheck.ts
      to: '@filelinks/core'
      via: classifyStagedLinks + getAgentProvider().run
      pattern: shouldRunAgentForLink|buildAgentPrompt|getStagedDiffForPaths
    - from: packages/cli/src/lib/runCheck.ts
      to: packages/cli/src/lib/runCheck.ts
      via: matchStagedLinks only in violation branch
      pattern: matchStagedLinks
---

<objective>
Implement **async** `runCheck` agent orchestration: violations first, then sequential **`provider.run`** for policy-eligible links using core prompt helpers (**CLI-06**, **CLI-07**).

**Out of scope:** Commander `--run-agents` flag and JSON envelope extension (07-03); docs (07-04). Add `runAgents?: boolean` to opts defaulting `false` so existing sync callers can be updated in 07-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-check-run-agents/07-RESEARCH.md
@.planning/phases/05-agent-policy-schema/05-CONTEXT.md
@packages/cli/src/lib/runCheck.ts
@packages/core/src/lib/agentRunPolicy.ts
@packages/core/src/lib/stagedClassifier.ts
@packages/core/src/lib/agentConfigResolver.ts
@packages/core/src/lib/providers/registry.ts
</context>

<interfaces>
Eligibility (per D-06, CLI-06):
```typescript
const coverageRows = classifyStagedLinks(staged, links);
const eligible = coverageRows.filter((cov) => {
  const policy = resolveAgentRunPolicy(config, cov.entry);
  return shouldRunAgentForLink(cov, policy);
});
// Do NOT filter on cov.missingAffected.length
```

Agent run (Phase 6):

```typescript
const agentConfig = resolveAgentConfig(config, cov.entry);
const provider = getAgentProvider(agentConfig.provider);
await provider.run({ prompt, config: agentConfig });
```

Prompt build (07-01):

```typescript
const prompt = buildAgentPrompt({
  prompt: resolvePrompt(config, cov.entry),
  coverage: cov,
  triggerDiff: getStagedDiffForPaths(opts.cwd, cov.triggerPaths),
  affectedFiles: readAffectedContents(opts.cwd, cov.entry.affects),
});
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Async runCheck signature and violation path</name>
  <files>packages/cli/src/lib/runCheck.ts,packages/cli/src/lib/cli.spec.ts</files>
  <read_first>packages/cli/src/lib/runCheck.ts,packages/cli/src/lib/cli.spec.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <action>
Refactor **`runCheck`**:

1. Extend **`RunCheckOpts`** with `runAgents?: boolean` (default false).

2. Change to **`export async function runCheck(opts: RunCheckOpts): Promise<number>`**.

3. Keep **`loadFileLinksConfig`** destructuring **`{ links, config }`** — pass **`config`** to resolvers (not links-only).

4. Preserve existing violation loop (`matchStagedLinks` → `violations` → human log or defer JSON to 07-03 when `runAgents` false).

5. When **`runAgents` is false**, return violation exit code immediately after printing (same semantics as today).

6. Do **not** import `@cursor/sdk` in CLI.

Update **`cli.spec.ts`** `runCheck` tests to **`await runCheck(...)`** (async).
</action>
<verify>
<automated>pnpm exec nx run cli:test --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/cli/src/lib/runCheck.ts` contains `export async function runCheck` - `packages/cli/src/lib/runCheck.ts` contains `runAgents` - `packages/cli/src/lib/runCheck.ts` contains `const { links, config }` or `loaded.config` - `packages/cli/src/lib/cli.spec.ts` contains `await runCheck`
</acceptance_criteria>
<done>runCheck is async; violation-only path unchanged when runAgents false</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent eligibility loop and provider.run</name>
  <files>packages/cli/src/lib/runCheck.ts</files>
  <read_first>packages/core/src/lib/agentRunPolicy.ts,packages/core/src/lib/stagedClassifier.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <behavior>
    - After violations printed, if `runAgents`: compute eligible via classify + shouldRunAgentForLink.
    - Never gate eligibility on `matchStagedLinks` or `missingAffected.length`.
    - Sequential `for (const cov of eligible)` await provider.run.
    - Catch errors → `normalizeError` → `console.error` → agentExit = 1.
    - Return `Math.max(violationExit, agentExit)`.
    - Human mode: optional progress line per link to stderr (e.g. trigger pattern + runId).
  </behavior>
  <action>
When **`opts.runAgents`** is true, after violation reporting:

1. `const coverageRows = classifyStagedLinks(staged, links)`.

2. Filter with **`resolveAgentRunPolicy(config, cov.entry)`** + **`shouldRunAgentForLink(cov, policy)`**.

3. For each eligible `cov`:
   - `resolveAgentConfig(config, cov.entry)`
   - `getAgentProvider(agentConfig.provider)`
   - Build prompt via **`resolvePrompt`**, **`getStagedDiffForPaths(opts.cwd, cov.triggerPaths)`**, **`readAffectedContents(opts.cwd, cov.entry.affects)`**, **`buildAgentPrompt`**
   - `await provider.run({ prompt, config: agentConfig })`

4. Accumulate **`agentRuns: AgentRunSummaryJson[]`** in `runCheck` for each eligible run (fields: `trigger`, `status: 'ok' | 'error'`, optional `runId` from `AgentRunResult`). Pass the array to `printCheckJson` when `opts.json && opts.runAgents` (07-03 extends formatter; define `AgentRunSummaryJson` in `formatters.ts` in this task if not present).

**Do not** skip agent when `cov.missingAffected.length > 0`.
</action>
<verify>
<automated>pnpm exec nx run cli:test --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/cli/src/lib/runCheck.ts` contains `classifyStagedLinks` - `packages/cli/src/lib/runCheck.ts` contains `shouldRunAgentForLink` - `packages/cli/src/lib/runCheck.ts` contains `buildAgentPrompt` - `packages/cli/src/lib/runCheck.ts` contains `getAgentProvider` - `packages/cli/src/lib/runCheck.ts` contains `agentRuns` - `packages/cli/src/lib/runCheck.spec.ts` contains `missingAffected` and `getAgentProvider`
</acceptance_criteria>
<done>Agent orchestration wired with correct eligibility source</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: runCheck.spec agent and exit-code tests</name>
  <files>packages/cli/src/lib/runCheck.spec.ts,packages/cli/src/lib/cli.spec.ts</files>
  <read_first>packages/cli/src/lib/cli.spec.ts,packages/core/src/lib/agentRunPolicy.spec.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <behavior>
    - Mock `classifyStagedLinks` returns coverage with `missingAffected.length > 0`, `shouldRunAgentForLink` true → `getAgentProvider().run` called once.
    - `provider.run` rejects → exit 1 even if violations warn-only.
    - `matchStagedLinks` returns [] but classify eligible → run still called (affect-only / D-06).
  </behavior>
  <action>
Create **`runCheck.spec.ts`** with `vi.mock('@filelinks/core')` hoisted mocks for:
`loadFileLinksConfig`, `getStagedFilePaths`, `matchStagedLinks`, `classifyStagedLinks`, `resolveAgentRunPolicy`, `shouldRunAgentForLink`, `resolvePrompt`, `resolveAgentConfig`, `getStagedDiffForPaths`, `readAffectedContents`, `buildAgentPrompt`, `getAgentProvider`, `normalizeError`.

Tests:

1. **CLI-06:** trigger matched, `missingAffected` non-empty, `runAgents: true` → `run` mock called.
2. **D-06:** `matchStagedLinks` returns `[]`, classify affect-only eligible → `run` called.
3. **Exit:** warn violations + failed `run` → returns `1`.

Keep existing **`cli.spec.ts`** violation tests passing with async `runCheck`.
</action>
<verify>
<automated>pnpm exec nx run cli:test --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/cli/src/lib/runCheck.spec.ts` exists - `packages/cli/src/lib/runCheck.spec.ts` contains `missingAffected` - `packages/cli/src/lib/runCheck.spec.ts` contains `getAgentProvider` - `pnpm exec nx run cli:test --skip-nx-cache` exits 0
</acceptance_criteria>
<done>CLI-06 and exit combining covered by unit tests</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run cli:test --skip-nx-cache`
- `pnpm exec nx run cli:build --skip-nx-cache`
</verification>

<success_criteria>

- Agents run only when `runAgents: true` (flag wired in 07-03)
- CLI-06 and CLI-07 orchestration complete in runCheck
  </success_criteria>

<output>
After completion, create `.planning/phases/07-cli-check-run-agents/07-02-SUMMARY.md`
</output>
