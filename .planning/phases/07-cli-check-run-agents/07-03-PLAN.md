---
phase: 07-cli-check-run-agents
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - packages/cli/src/lib/cli.ts
  - packages/cli/src/lib/formatters.ts
  - packages/cli/src/lib/runCheck.ts
  - packages/cli/src/lib/cli.e2e.spec.ts
  - packages/cli/src/lib/cli.spec.ts
autonomous: true
requirements:
  - CLI-05
must_haves:
  truths:
    - 'filelinks check --run-agents passes runAgents true into runCheck.'
    - 'check action awaits async runCheck and sets process.exitCode.'
    - '--json --run-agents emits one JSON object with violations and agentRuns.'
    - 'E2E proves --run-agents reaches check flow without live Cursor API.'
  artifacts:
    - path: packages/cli/src/lib/cli.ts
      provides: Commander --run-agents on check
      contains: '--run-agents'
    - path: packages/cli/src/lib/formatters.ts
      provides: extended check JSON printer
      contains: agentRuns
    - path: packages/cli/src/lib/cli.e2e.spec.ts
      provides: '[e2e]' flag wiring test
      contains: '--run-agents'
  key_links:
    - from: packages/cli/src/lib/cli.ts
      to: packages/cli/src/lib/runCheck.ts
      via: await runCheck({ runAgents })
      pattern: runAgents
    - from: packages/cli/src/lib/runCheck.ts
      to: packages/cli/src/lib/formatters.ts
      via: printCheckJson when json
      pattern: printCheckJson
---

<objective>
Wire **CLI-05**: Commander **`--run-agents`** on `check`, async action, JSON envelope with **`agentRuns`**, and **[e2e]** flag propagation test.

**Depends on:** 07-02 async `runCheck` with agent loop.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-check-run-agents/07-RESEARCH.md
@.cursor/rules/filelinks-cli-e2e.mdc
@packages/cli/src/lib/cli.ts
@packages/cli/src/lib/formatters.ts
@packages/cli/src/lib/cli.e2e.spec.ts
@packages/cli/src/lib/runAdd.ts
</context>

<interfaces>
Recommended JSON shape (research):
```typescript
export type AgentRunSummaryJson = {
  trigger: string;
  status: 'finished' | 'failed';
  runId?: string;
  error?: string;
};

// stdout: { violations: CheckViolationJson[], agentRuns?: AgentRunSummaryJson[] }

````

Commander pattern (mirror `runAdd` async):
```typescript
.option('--run-agents', 'Run agents for policy-eligible links after check')
.action(async function (this: Command) {
  process.exitCode = await runCheck({ ..., runAgents: Boolean(opts.runAgents) });
});
````

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Commander --run-agents and async check action</name>
  <files>packages/cli/src/lib/cli.ts</files>
  <read_first>packages/cli/src/lib/cli.ts,packages/cli/src/lib/runAdd.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <action>
On **`program.command('check')`**:

1. Add **`.option('--run-agents', 'Run agents for policy-eligible links after check')`**.

2. Change action to **`async function`**: read `this.opts()` for `runAgents`, call **`process.exitCode = await runCheck({ cwd, configPath, json, runAgents: Boolean(opts.runAgents) })`**.

3. Keep try/catch + `normalizeError` pattern consistent with `runAdd`.

Update **`cli.ts`** only; ensure no sync `runCheck` call remains.
</action>
<verify>
<automated>pnpm exec nx run cli:build --skip-nx-cache</automated>
</verify>
<acceptance_criteria> - `packages/cli/src/lib/cli.ts` contains `'--run-agents'` - `packages/cli/src/lib/cli.ts` contains `await runCheck` - `packages/cli/src/lib/cli.ts` contains `runAgents`
</acceptance_criteria>
<done>CLI-05 flag wired to runCheck</done>
</task>

<task type="auto">
  <name>Task 2: JSON envelope with agentRuns</name>
  <files>packages/cli/src/lib/formatters.ts,packages/cli/src/lib/runCheck.ts</files>
  <read_first>packages/cli/src/lib/formatters.ts,packages/cli/src/lib/runCheck.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <action>
1. In **`formatters.ts`**, add **`AgentRunSummaryJson`** type and extend **`printCheckJson`** to accept optional second argument or new signature:
   **`printCheckJson(violations, agentRuns?: AgentRunSummaryJson[])`** → `JSON.stringify({ violations, ...(agentRuns ? { agentRuns } : {}) })`.
   Preserve **`violations`** key for backward compatibility when `agentRuns` omitted.

2. In **`runCheck.ts`**, when **`opts.json`**: defer **all** stdout until end; accumulate **`agentRuns`** during agent loop (status `finished` with `runId`, or `failed` with `error` message from `normalizeError`).

3. Human mode (`!json`): keep violation lines on stdout; agent progress/errors to **stderr** only.

4. Single **`printCheckJson`** call at end when json true.
   </action>
   <verify>
   <automated>pnpm exec nx run cli:test --skip-nx-cache</automated>
   </verify>
   <acceptance_criteria> - `packages/cli/src/lib/formatters.ts` contains `agentRuns` - `packages/cli/src/lib/formatters.ts` contains `AgentRunSummaryJson` or `agentRuns?:` - `packages/cli/src/lib/runCheck.ts` contains `printCheckJson` - `packages/cli/src/lib/runCheck.spec.ts` or `cli.spec.ts` asserts JSON includes `agentRuns` when runAgents true (add test if missing)
   </acceptance_criteria>
   <done>Machine-readable check output includes agent run summaries</done>
   </task>

<task type="auto">
  <name>Task 3: E2E --run-agents wiring</name>
  <files>packages/cli/src/lib/cli.e2e.spec.ts</files>
  <read_first>packages/cli/src/lib/cli.e2e.spec.ts,.cursor/rules/filelinks-cli-e2e.mdc,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <action>
Implement **`describe('[e2e] check --run-agents', ...)`** per `.cursor/rules/filelinks-cli-e2e.mdc`:

- **Do not** `vi.mock('./runCheck.js')` — keep command-boundary testing via `runCli`.
- Extend existing **`vi.mock('@filelinks/core')`** in `cli.e2e.spec.ts` with no-op agent path: `classifyStagedLinks` → `[]`, `getAgentProvider` → `{ validateCredentials: () => {}, listModels: async () => [], run: async () => ({ status: 'finished' }) }`, plus stubs for `resolveAgentRunPolicy`, `shouldRunAgentForLink`, `resolveAgentConfig`, `resolvePrompt`, `getStagedDiffForPaths`, `readAffectedContents`, `buildAgentPrompt` as needed so `runCheck` completes without live API.

Assert **CLI-05 wiring** by stdout/exit or by spying **`getAgentProvider`** after `runCli(['node', 'filelinks', '--cwd', fixture, 'check', '--run-agents'])` — e.g. `expect(getAgentProvider).toHaveBeenCalled()` when classify returns one eligible row (configure mock `classifyStagedLinks` to return minimal coverage + `shouldRunAgentForLink` true).

Keep other `[e2e]` describes unchanged; isolate new mock overrides in the new describe block if needed.

Run **`pnpm run cli:test:e2e`**.
</action>
<verify>
<automated>pnpm run cli:test:e2e</automated>
</verify>
<acceptance_criteria> - `packages/cli/src/lib/cli.e2e.spec.ts` contains `[e2e]` and `--run-agents` - `packages/cli/src/lib/cli.e2e.spec.ts` contains `runAgents: true` or `runAgents:true` - `pnpm run cli:test:e2e` exits 0
</acceptance_criteria>
<done>CLI-05 verified at command boundary without live API</done>
</task>

</tasks>

<verification>
- `pnpm exec nx run cli:test --skip-nx-cache`
- `pnpm run cli:test:e2e`
- `pnpm exec nx run cli:build --skip-nx-cache`
</verification>

<success_criteria>

- `filelinks check --run-agents` is the supported opt-in entry point
- JSON and E2E contracts stable for CI
  </success_criteria>

<output>
After completion, create `.planning/phases/07-cli-check-run-agents/07-03-SUMMARY.md`
</output>
