---
phase: 07-cli-check-run-agents
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - 'README documents check --run-agents and when agents run (policy).'
    - 'README shows agent config example with local and cloud runtime.'
    - 'README documents CURSOR_API_KEY.'
    - 'CONTRIBUTING points contributors to agent smoke setup.'
  artifacts:
    - path: README.md
      provides: user-facing agent docs
      contains: '--run-agents'
    - path: CONTRIBUTING.md
      provides: contributor agent setup
      contains: CURSOR_API_KEY
  key_links:
    - from: README.md
      to: packages/cli/src/lib/cli.ts
      via: documented flag matches implementation
      pattern: --run-agents
---

<objective>
**DOC-03:** Document **`filelinks check --run-agents`**, agent config shape, **`CURSOR_API_KEY`**, and local vs cloud runtime in README and CONTRIBUTING.

**Depends on:** 07-03 so docs match shipped CLI/JSON behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-cli-check-run-agents/07-RESEARCH.md
@packages/core/src/lib/schema.ts
@README.md
@CONTRIBUTING.md
</context>

<interfaces>
Config shape (from schema — document, do not duplicate literals in prose incorrectly):
```typescript
// FileLinkConfig.agent + FileLinkEntry.agent
agent?: {
  runPolicy?: 'trigger-or-affects';
  provider: 'cursor';
  runtime: 'local' | 'cloud';
  model: string;
  local?: { cwd: string };
  cloud?: { repos: string[] };
};
```

Out of scope reminders for docs: standalone `suggest`, auto-run without flag, non-Cursor providers.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: README agent section</name>
  <files>README.md</files>
  <read_first>README.md,.planning/REQUIREMENTS.md,packages/core/src/lib/schema.ts,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <action>
Add a **"AI agents (v1.1)"** or **"Running agents"** section to README:

1. **`filelinks check --run-agents`** — opt-in; runs check violations first, then agents for links matching **`trigger-or-affects`** (default) via staged classification.

2. **When agents run:** trigger and/or affect paths staged; **missing companions do not block** agent run.

3. **`CURSOR_API_KEY`** env var required for Cursor provider.

4. **Config example** `defineLinks` block with global `agent` and per-link override showing:
   - `runtime: 'local'` + `local: { cwd: '...' }`
   - `runtime: 'cloud'` + `cloud: { repos: ['org/repo'] }`

5. Mention **`--json`** includes optional **`agentRuns`** array when `--run-agents` is set.

Keep tone consistent with existing README; link to CONTRIBUTING for dev workflow.
</action>
<verify>
<automated>grep -q -- '--run-agents' README.md && grep -q -- 'CURSOR_API_KEY' README.md</automated>
</verify>
<acceptance_criteria> - `README.md` contains `--run-agents` - `README.md` contains `CURSOR_API_KEY` - `README.md` contains `trigger-or-affects` or `runPolicy` - `README.md` contains `local` and `cloud` in agent context
</acceptance_criteria>
<done>User-facing DOC-03 content in README</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING agent smoke and testing notes</name>
  <files>CONTRIBUTING.md</files>
  <read_first>CONTRIBUTING.md,AGENTS.md,.planning/phases/07-cli-check-run-agents/07-RESEARCH.md</read_first>
  <action>
In **CONTRIBUTING.md** (documentation map section or new subsection):

1. **Manual smoke:** `CURSOR_API_KEY`, `filelinks check --run-agents`, local `agent.local.cwd` pointing at repo under test.

2. **CI:** unit/E2E use mocks — no live Cursor in CI (reference `cli.e2e.spec.ts` and `cursorProvider.spec.ts`).

3. **Build/link loop:** point to existing linked-consumer workflow when testing CLI against external project.

4. Cross-link README agent section.

Do not document deferred features (git-hook package, `suggest` command).
</action>
<verify>
<automated>grep -q -- 'CURSOR_API_KEY' CONTRIBUTING.md && grep -q -- 'run-agents' CONTRIBUTING.md</automated>
</verify>
<acceptance_criteria> - `CONTRIBUTING.md` contains `CURSOR_API_KEY` - `CONTRIBUTING.md` contains `run-agents` or `--run-agents` - `CONTRIBUTING.md` contains `cursorProvider` or `mock` (CI note)
</acceptance_criteria>
<done>Contributor DOC-03 content complete</done>
</task>

</tasks>

<verification>
- `grep -E 'run-agents|CURSOR_API_KEY' README.md CONTRIBUTING.md`
- Spot-check examples against `packages/core/src/lib/schema.ts` agent fields
</verification>

<success_criteria>

- DOC-03 satisfied; docs match Phase 7 CLI behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/07-cli-check-run-agents/07-04-SUMMARY.md`
</output>
