# git-hook

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build git-hook` to build the library.

## Running unit tests

Run `nx test git-hook` to execute the unit tests via [Vitest](https://vitest.dev/).
